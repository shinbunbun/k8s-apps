apiVersion: v1
kind: Service
metadata:
  name: home-assistant
  labels:
    app: home-assistant  # VMServiceScrape (victoriametrics-config) のセレクタ対象
spec:
  selector:
    app: home-assistant
  ports:
    - name: http
      port: 8123
      targetPort: http
