# KSOPS generator (ArgoCD repo-server が kustomize build 時に secret.enc.yaml を復号)
apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: hermes-secret-generator
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
  - secrets/secret.enc.yaml
