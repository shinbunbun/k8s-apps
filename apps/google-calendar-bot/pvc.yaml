apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: google-calendar-bot-sqlite
spec:
  storageClassName: piraeus-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: google-calendar-bot-storage
spec:
  storageClassName: piraeus-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
