# Rust + Polars: bronze (raw -> daily-struct) for macos-unified source
#
# shinbunbun/dotfiles-private#191 Phase 4 で apps/logs-typed-dbt/ から切り出し。
# 旧 dbt-duckdb (apps/logs-typed-dbt/cronjob-bronze.yaml の SOURCES に macos-unified
# 含む) との並走 verify を継続。新 prefix `daily-struct/source=macos-unified/` に書く
# ので旧 `daily/` には触れない。
# image source: shinbunbun/logs-pipeline
# credentials: logs-rust-pipeline-credentials (apps/garage/keys.yaml の専用 GarageKey)
#
# Schedule:
#   01:05 JST (旧 dbt-duckdb bronze 00:30 / 新 systemd bronze 00:35 / 新 routeros
#   bronze 00:45 / 新 k8s-pods bronze 00:55 から 10 分ずつずらした連番)。raw/ を
#   読むだけで delete はしないので衝突は理論上ないが、観測しやすく時間差。
#
# Memory:
#   macos-unified は fluent-bit Lua filter で /nix/store/* と /kernel のみ通す
#   ホワイトリスト方式のため volume 小 (systemd 同等以下を想定、16K-100K 行/日)。
#   schema は flat (k8s-pods の OTLP attributes STRUCT のような落とし穴なし)。
#   よって systemd と同じ 4Gi で start。実測 peak_mib 取得後に縮小可。
apiVersion: batch/v1
kind: CronJob
metadata:
  name: logs-rust-pipeline-bronze-macos-unified
spec:
  schedule: "5 1 * * *"
  timeZone: Asia/Tokyo
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  startingDeadlineSeconds: 3600
  jobTemplate:
    spec:
      backoffLimit: 2
      activeDeadlineSeconds: 3600
      template:
        metadata:
          labels:
            app: logs-rust-pipeline
            tier: bronze
            source: macos-unified
        spec:
          restartPolicy: OnFailure
          containers:
            - name: pipeline
              image: ghcr.io/shinbunbun/logs-pipeline:latest
              args:
                - bronze
                - --source
                - macos-unified
                # date は省略 -> binary 内で yesterday-jst を解釈
              env:
                - name: AWS_S3_ENDPOINT
                  value: "http://garage.garage.svc.cluster.local:3900"
                - name: RUST_LOG
                  value: "info"
              envFrom:
                - secretRef:
                    name: logs-rust-pipeline-credentials
              resources:
                requests:
                  cpu: 100m
                  memory: 256Mi
                limits:
                  memory: 4Gi
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
                runAsNonRoot: true
                capabilities:
                  drop: [ALL]
              volumeMounts:
                # tempfile crate が /tmp に hour 別 NDJSON + 最終 Parquet を書くため
                # emptyDir をマウント。zstd 解凍後 NDJSON が一時的に展開される。
                - name: tmp
                  mountPath: /tmp
          volumes:
            - name: tmp
              emptyDir:
                sizeLimit: 1Gi
