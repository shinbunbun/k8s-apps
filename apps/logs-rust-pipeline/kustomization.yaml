# logs-rust-pipeline: Rust + Polars 版 logs archive pipeline
#
# Phase 4 (現状): raw/ NDJSON.zst → daily-struct/ Parquet (bronze、Polars dynamic
#                 NDJSON inference、faithful raw archive)
# Phase 5 (#197): daily-struct/ → silver/ (per-source canonical schema、ts 正規化、
#                 dashboard 用 lean schema、union_by_name=false で query 可能)
#
# ApplicationSet (apps/* git generator、applicationsets/apps-appset.yaml) が自動検出
# して ArgoCD Application を生成。namespace は basename と一致する `logs-rust-pipeline`
# に CreateNamespace=true で作られる。credentials Secret は apps/garage/keys.yaml の
# `logs-rust-pipeline` GarageKey から secretTemplate で `logs-rust-pipeline-credentials`
# として cross-namespace 配布。
#
# verify-job-template.yaml は手動 `kubectl create -f` 専用なので resources には含めない。
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: logs-rust-pipeline
resources:
  - cronjob-bronze-systemd.yaml
  - cronjob-bronze-routeros.yaml
  - cronjob-bronze-k8s-pods.yaml
  - cronjob-bronze-macos-unified.yaml
  - cronjob-silver-systemd.yaml
  - cronjob-silver-routeros.yaml
  - cronjob-silver-k8s-pods.yaml
  - cronjob-silver-macos-unified.yaml

# Image SHA pin (Issue #191 Phase 4)
#
# 4 CronJob 共通の image を `:latest` から固定 SHA tag (logs-pipeline build workflow
# で生成される `type=sha,format=short` 7-char SHA) に kustomize 側で書き換える。
# CronJob YAML の `image:` field は `:latest` のまま残し、ここで一元管理。
#
# ArgoCD Image Updater は CronJob 非対応のため手動 bump 運用 (補遺 8 の判定 B)。
# logs-pipeline 側で新しい SHA をデプロイしたいときの手順:
#   1. logs-pipeline で feature branch -> PR -> main merge (workflow が SHA tag push)
#   2. 本ファイルの newTag を新 SHA に更新する PR を切る
#   3. ArgoCD sync で 4 CronJob 全てが新 image で fire
images:
  - name: ghcr.io/shinbunbun/logs-pipeline
    newTag: "2f002d7"  # logs-pipeline main HEAD: 2f002d7... (2026-05-05, PR #17 Unknown variant + --dst-source flag)
