apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: obsidian-livesync-data
spec:
  storageClassName: piraeus-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
