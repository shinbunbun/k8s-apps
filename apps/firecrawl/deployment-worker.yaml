# Firecrawl worker - BullMQ queue worker (Redis-backed)
#
# Service 不要 (HTTP server を expose せず、liveness probe 用に :3005 のみ listen)。
# api と同 image、CMD で queue-worker.js に分岐。
apiVersion: apps/v1
kind: Deployment
metadata:
  name: firecrawl-worker
  labels:
    app: firecrawl-worker
    tier: firecrawl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: firecrawl-worker
  template:
    metadata:
      labels:
        app: firecrawl-worker
        tier: firecrawl
    spec:
      containers:
        - name: worker
          image: ghcr.io/firecrawl/firecrawl@sha256:ee3074392fd920d11f595d2088aea8a09cef4001c3a81c47ba04b43cb379245c
          command: ["node"]
          args:
            - "--max-old-space-size=768"
            - "dist/src/services/queue-worker.js"
          ports:
            - name: liveness
              containerPort: 3005
          env:
            - name: PORT
              value: "3005"
          envFrom:
            - configMapRef:
                name: firecrawl-config
            - secretRef:
                name: firecrawl-credentials
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
          startupProbe:
            httpGet:
              path: /liveness
              port: liveness
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 24
          livenessProbe:
            httpGet:
              path: /liveness
              port: liveness
            periodSeconds: 30
            failureThreshold: 3
      terminationGracePeriodSeconds: 60
