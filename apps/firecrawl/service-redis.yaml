# Redis Service - BullMQ queue + rate-limit 用
apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: redis
    tier: firecrawl
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
      protocol: TCP
