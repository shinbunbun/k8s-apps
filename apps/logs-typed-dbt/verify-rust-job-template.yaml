# Rust + Polars 版 PoC: verify (旧 daily/ vs 新 daily-struct/) Job template
#
# kustomize 外。手動 `kubectl create -f apps/logs-typed-dbt/verify-rust-job-template.yaml`
# で実行する。`--source` (systemd / routeros) と `--date` (TARGET_DATE) を都度書き
# 換えること (sed 推奨、両方を編集してから create)。
#
# verify subcommand は logs-pipeline binary に実装済み:
#   logs-pipeline verify --source systemd --date YYYY-MM-DD \
#     --old-prefix daily --new-prefix daily-struct
# 旧 dbt-duckdb は `daily/date=*/source=*/` (date 先) で書き、新 Rust は
# `daily-struct/source=*/date=*/` (source 先) で書く。binary 側で path 順序差を吸収。
apiVersion: batch/v1
kind: Job
metadata:
  generateName: logs-rust-pipeline-verify-systemd-
  namespace: logs-typed-dbt
spec:
  backoffLimit: 0
  activeDeadlineSeconds: 600
  # 失敗時 Pod が Failed のまま残ると alert (KubePodNotReady) を発火させるため、
  # Complete/Failed どちらでも 10 分後に Job + Pod を自動 GC。ログは事前に
  # `kubectl logs` で取り出すか、retain したいなら create 時に削っておく。
  ttlSecondsAfterFinished: 600
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: verify
          image: ghcr.io/shinbunbun/logs-pipeline:latest
          args:
            - verify
            - --source
            - systemd
            - --date
            - "2026-05-02"  # ← edit before kubectl create
            - --old-prefix
            - daily
            - --new-prefix
            - daily-struct
          env:
            - name: AWS_S3_ENDPOINT
              value: "http://garage.garage.svc.cluster.local:3900"
            - name: RUST_LOG
              value: "info"
          envFrom:
            - secretRef:
                name: logs-typed-dbt-credentials
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities:
              drop: [ALL]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 512Mi
