apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cloudflared-k3s
resources:
  - deployment.yaml
generators:
  - secret-generator.yaml
configMapGenerator:
  - name: cloudflared-config
    files:
      - config.yaml
