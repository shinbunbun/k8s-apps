apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tokyu-flyer-bot-data
spec:
  storageClassName: piraeus-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
