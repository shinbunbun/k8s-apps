# Scanopy PostgreSQL - ClusterIP Service (server Deployment から参照)。
apiVersion: v1
kind: Service
metadata:
  name: scanopy-postgres
  labels:
    app: scanopy-postgres
spec:
  selector:
    app: scanopy-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
