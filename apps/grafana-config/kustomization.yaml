# Grafana Config - Kustomize エントリポイント
#
# ApplicationSet (applicationsets/apps-appset.yaml) が apps/grafana-config を
# 自動検出して ArgoCD Application を生成する。
# すべてのリソースを grafana namespace にデプロイ。
#
# configMapGenerator で dashboards/*.json を ConfigMap 化し、
# label grafana_dashboard=1 を付与することで Helm chart の dashboard sidecar が
# 自動ロードする (infrastructure/grafana/application.yaml の
# sidecar.dashboards.label / labelValue / searchNamespace 設定と対応)。
#
# commonAnnotations で argocd.argoproj.io/sync-options: ServerSideApply=true を
# 付与。理由: nixos.json ダッシュボードは 474KB あり、ArgoCD の client-side apply
# では kubectl.kubernetes.io/last-applied-configuration annotation に全データが
# コピーされ metadata.annotations 256KiB 上限を超過して ConfigMap apply が失敗する。
# server-side apply は last-applied-configuration を使わないためこの制限を回避できる。
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: grafana

commonAnnotations:
  argocd.argoproj.io/sync-options: ServerSideApply=true

resources:
  - postgres-cluster.yaml
  - datasources-configmap.yaml
  - lan-service.yaml
  - objectstore.yaml
  - scheduled-backup.yaml

generators:
  - secret-generator.yaml

configMapGenerator:
  - name: grafana-dashboard-overview
    files:
      - dashboards/overview.json
    options:
      labels:
        grafana_dashboard: "1"
      disableNameSuffixHash: false
  - name: grafana-dashboard-nixos
    files:
      - dashboards/nixos.json
    options:
      labels:
        grafana_dashboard: "1"
      disableNameSuffixHash: false
  - name: grafana-dashboard-routeros
    files:
      - dashboards/routeros.json
    options:
      labels:
        grafana_dashboard: "1"
      disableNameSuffixHash: false
  - name: grafana-dashboard-k8s
    files:
      - dashboards/k8s.json
    options:
      labels:
        grafana_dashboard: "1"
      annotations:
        grafana_folder: Kubernetes
      disableNameSuffixHash: false
  - name: grafana-dashboard-logs-overview
    files:
      - dashboards/logs-overview.json
    options:
      labels:
        grafana_dashboard: "1"
      disableNameSuffixHash: false
  - name: grafana-dashboard-logs-errors
    files:
      - dashboards/logs-errors.json
    options:
      labels:
        grafana_dashboard: "1"
      disableNameSuffixHash: false
  - name: grafana-dashboard-logs-explore
    files:
      - dashboards/logs-explore.json
    options:
      labels:
        grafana_dashboard: "1"
      disableNameSuffixHash: false
  - name: grafana-dashboard-cilium
    files:
      - dashboards/cilium.json
    options:
      labels:
        grafana_dashboard: "1"
      annotations:
        grafana_folder: Kubernetes
      disableNameSuffixHash: false
  - name: grafana-dashboard-hubble
    files:
      - dashboards/hubble.json
    options:
      labels:
        grafana_dashboard: "1"
      annotations:
        grafana_folder: Kubernetes
      disableNameSuffixHash: false
  - name: grafana-dashboard-hubble-network-overview-namespace
    files:
      - dashboards/hubble-network-overview-namespace.json
    options:
      labels:
        grafana_dashboard: "1"
      annotations:
        grafana_folder: Kubernetes
      disableNameSuffixHash: false
  - name: grafana-dashboard-node-exporter-full
    files:
      - dashboards/node-exporter-full.json
    options:
      labels:
        grafana_dashboard: "1"
      annotations:
        grafana_folder: Kubernetes
      disableNameSuffixHash: false
  - name: grafana-dashboard-k8s-views-global
    files:
      - dashboards/k8s-views-global.json
    options:
      labels:
        grafana_dashboard: "1"
      annotations:
        grafana_folder: Kubernetes
      disableNameSuffixHash: false
  - name: grafana-dashboard-k8s-views-namespaces
    files:
      - dashboards/k8s-views-namespaces.json
    options:
      labels:
        grafana_dashboard: "1"
      annotations:
        grafana_folder: Kubernetes
      disableNameSuffixHash: false
  - name: grafana-dashboard-k8s-views-nodes
    files:
      - dashboards/k8s-views-nodes.json
    options:
      labels:
        grafana_dashboard: "1"
      annotations:
        grafana_folder: Kubernetes
      disableNameSuffixHash: false
  - name: grafana-dashboard-k8s-views-pods
    files:
      - dashboards/k8s-views-pods.json
    options:
      labels:
        grafana_dashboard: "1"
      annotations:
        grafana_folder: Kubernetes
      disableNameSuffixHash: false
  - name: grafana-dashboard-k8s-system-api-server
    files:
      - dashboards/k8s-system-api-server.json
    options:
      labels:
        grafana_dashboard: "1"
      annotations:
        grafana_folder: Kubernetes
      disableNameSuffixHash: false
  - name: grafana-dashboard-victoriametrics-cluster
    files:
      - dashboards/victoriametrics-cluster.json
    options:
      labels:
        grafana_dashboard: "1"
      annotations:
        grafana_folder: Kubernetes
      disableNameSuffixHash: false
  - name: grafana-dashboard-vmagent
    files:
      - dashboards/vmagent.json
    options:
      labels:
        grafana_dashboard: "1"
      annotations:
        grafana_folder: Kubernetes
      disableNameSuffixHash: false
  - name: grafana-dashboard-vmalert
    files:
      - dashboards/vmalert.json
    options:
      labels:
        grafana_dashboard: "1"
      annotations:
        grafana_folder: Kubernetes
      disableNameSuffixHash: false
  - name: grafana-dashboard-argocd-application-overview
    files:
      - dashboards/argocd-application-overview.json
    options:
      labels:
        grafana_dashboard: "1"
      disableNameSuffixHash: false
  - name: grafana-dashboard-argocd-operational-overview
    files:
      - dashboards/argocd-operational-overview.json
    options:
      labels:
        grafana_dashboard: "1"
      disableNameSuffixHash: false
  - name: grafana-dashboard-storage-capacity
    files:
      - dashboards/storage-capacity.json
    options:
      labels:
        grafana_dashboard: "1"
      disableNameSuffixHash: false
  - name: grafana-dashboard-smartctl-exporter
    files:
      - dashboards/smartctl-exporter.json
    options:
      labels:
        grafana_dashboard: "1"
      disableNameSuffixHash: false
  # DuckDB datasource (motherduck plugin) で Garage 上の長期 Parquet を集計する
  # 「Logs Archive」フォルダのダッシュボード群。短期 Loki ダッシュボード
  # (logs-errors / logs-overview / logs-explore) との重複を避けるため別フォルダに
  # 分離している。issue #654 参照。
  - name: grafana-dashboard-routeros-archive
    files:
      - dashboards/routeros-archive.json
    options:
      labels:
        grafana_dashboard: "1"
      annotations:
        grafana_folder: Logs Archive
      disableNameSuffixHash: false
  - name: grafana-dashboard-k8s-pods-archive
    files:
      - dashboards/k8s-pods-archive.json
    options:
      labels:
        grafana_dashboard: "1"
      annotations:
        grafana_folder: Logs Archive
      disableNameSuffixHash: false
